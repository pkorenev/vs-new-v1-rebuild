<% if @object.respond_to?(:resource) && @object.resource %>
  <%= link_to(@object.resource.name, edit_path(model_name: @object.resource.rails_admin_model_to_param, id: @object.resource.id)) %>
<% end %>	